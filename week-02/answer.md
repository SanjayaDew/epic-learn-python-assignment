# Week 02 Answers

## Question 1
I created four variable with different data types(string, integer, float, boolean). I used type() function to display the data type of each variable.

## Question 2
I performed string operations such as accessing characters using indexing, slicing to extract word, converting to uppercase[.upper()] and replacing a word[.replace()] using built in string methods.

## Question 3
I calculated total amount by multiplying price and quantity. If total is greater than 700, I applied 10% discount. Otherwise, I printed total without any discount.

## Question 4
I created a list of subjects. After, I used list methods like append() to add a new subject and remove() to delete an existing subjects. Then I printed the updated list and its length.

## Question 5
I used a set to remove duplicate values from the list. Because sets don't allow duplicates, converting the list to a set and back to a list gives unique values.